spring:
  application:
    name: LibrarySystem

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:library_db}
    username: ${DB_USERNAME:library_user}
    password: ${DB_PASSWORD:library_pass}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        default_schema: public

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: America/Bogota
    serialization:
      write-dates-as-timestamps: false

server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: /

application:
  jwt:
    secret: ${JWT_SECRET:VGhpc0lzQVZlcnlMb25nU2VjcmV0S2V5Rm9yTGlicmFyeVN5c3RlbTIwMjQ=}
    expiration: ${JWT_EXPIRATION:900000}        # 15 min en ms
    refresh-expiration: ${JWT_REFRESH_EXPIRATION:604800000}  # 7 días en ms

  library:
    max-renewals: 2
    loan-days-physical: 15        # US-011: 15 días
    loan-days-digital: 21         # US-012: 21 días
    fine-per-day: 1000.0          # US-019: $1000/día
    max-active-loans: 5           # US-011: máximo 5 físicos
    max-active-loans-physical: 5
    max-active-loans-digital: 3   # US-012: máximo 3 digitales
    max-active-reservations: 3    # US-016: máximo 3
    reservation-expiry-days: 3

    cors:
      allowed-origins: ${CORS_ALLOWED_ORIGINS:*}

logging:
  level:
    root: INFO
    com.library: DEBUG
    org.springframework.security: INFO
    org.hibernate.SQL: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: logs/library-system.log

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
    tagsSorter: alpha
    display-request-duration: true
  info:
    title: Library System API
    description: Sistema completo de gestión de biblioteca
    version: 1.0.0

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
